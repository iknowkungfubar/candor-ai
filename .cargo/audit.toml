[advisories]
# Known transitive dependency warnings (tracked for upstream fixes)
# All of these come from surrealdb / wasmtime transitive dependency trees.

ignore = [
    "RUSTSEC-2023-0089",  # atomic-polyfill 1.0.3 — unmaintained (transitive via surrealdb -> geo-types -> rstar -> heapless)
    "RUSTSEC-2025-0141",  # bincode 1.3.3 — unmaintained (transitive via surrealdb-core, surrealdb)
    "RUSTSEC-2025-0057",  # fxhash 0.2.1 — unmaintained (transitive via wasmtime -> fxprof-processed-profile)
    "RUSTSEC-2026-0002",  # lru 0.12.5 — unsound (transitive via surrealdb-core -> surrealkv)
    "RUSTSEC-2023-0071",  # rsa 0.9.10 — Marvin Attack (transitive via surrealdb-core -> jsonwebtoken, no fix available)
]
